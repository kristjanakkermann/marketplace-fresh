<h1>Items</h1>

Our marketplace has <%= Item.in_sales_count %> items in sales and counting!

<ul>
    <% @items.each do |item| %>
        <% unless item.sold? %>
        <li>
            <%= link_to item.name, item%>
        </li>
    <% end %>
<% end %>
</ul>

<%= link_to "New item", new_item_path %>